# Balancer Candle Server Configuration
PORT=3456

# The Graph API Key for Balancer V2 subgraph
GRAPH_API_KEY=YOUR_GRAPH_API_KEY_HERE

# Indexing window (days back from now to start indexing)
INDEX_DAYS_BACK=7

# Indexing interval (minutes between index runs)
INDEX_INTERVAL_MINUTES=5
